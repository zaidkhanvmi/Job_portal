import React from 'react'

const Dashboard = () => {

    // Total Jobs
    // Active Jobs
    // Applications Received
    // Rejected / Shortlisted counts
    // A line or bar chart → applications over time
    // A pie chart → job status distribution
    // Recent applications list (last 5)

  return (
    <div>Dashboard</div>
  )
}

export default Dashboard